<% unless current_user?(@listing.user) %>
  <div id="follow_form">
  <% if current_user.following?(@listing) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>