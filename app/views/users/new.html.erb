<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <%= render 'fields', f: f %>
      <div class="btn-group-wrap">
        <%= f.submit "Start Selling Now!", class: "btn btn-large btn-primary" %>
      </div>
    <% end %>
  </div>
</div>